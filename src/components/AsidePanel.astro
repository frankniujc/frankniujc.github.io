---
const { title = "Aside", children } = Astro.props;
---

<aside
  style="
    position: fixed;
    top: 6rem;
    right: 2rem;
    width: 200px;
    background: var(--astro-code-bg, #f8fafc);
    border-radius: 0.5rem;
    box-shadow: 0 2px 8px 0 rgba(0,0,0,0.06);
    padding: 1.5rem 1.25rem;
    z-index: 100;
  "
  class="hidden lg:block"
>
  <strong style="font-size:1.1em;">{title}</strong>
  <div style="margin-top:0.75em;">
    <div style="display: flex; justify-content: center; align-items: flex-start; gap: 2rem;">
        <main style="flex: 1; max-width: 700px;">
            <slot />
        </main>
    </div>
  </div>
</aside>

